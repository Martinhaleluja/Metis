## What this changes

<!-- One or two sentences. What is different now, and why. -->

## Why

<!-- The problem this solves. If it fixes an issue, link it. -->

## Checklist

- [ ] `dotnet build Metis.sln -c Debug` succeeds with no warnings
- [ ] `dotnet test Metis.sln -c Debug` passes
- [ ] I ran the app and used the thing I changed — a clean build is not
      verification for UI, capture, hotkeys or window behaviour
- [ ] If this touches capture, the accessibility scan, storage or an agent's
      permissions, I have said so below

## Privacy and security impact

<!--
  Delete if none. Otherwise: does this change what Metis captures, what it
  sends, what it stores, or what an agent may reach?
-->

None.
