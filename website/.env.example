# Supabase project that stores the launch waitlist.
# These are browser-safe: the publishable key can only reach the two
# waitlist RPCs, which never return anybody else's email address.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
